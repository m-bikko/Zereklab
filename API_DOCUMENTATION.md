# API Documentation - ZerekLab

## Обзор

ZerekLab API предоставляет полный функционал для управления интернет-магазином образовательных наборов, включая управление товарами, категориями, продажами, бонусной системой и многое другое.

## Доступ к документации

### Swagger UI
Интерактивная документация доступна по адресу:
```
http://localhost:3000/api/docs
```

### Swagger JSON
Спецификация OpenAPI 3.0 в формате JSON:
```
http://localhost:3000/api/swagger
```

## Основные группы endpoints

### 🔐 Authentication
- `POST /api/auth/login` - Аутентификация администратора
- `POST /api/auth/sales-login` - Аутентификация сотрудника продаж

### 📦 Products (Товары)
- `GET /api/products` - Получение списка товаров с фильтрацией и пагинацией
- `POST /api/products` - Создание нового товара
- `GET /api/products/{id}` - Получение товара по ID
- `PUT /api/products/{id}` - Обновление товара
- `DELETE /api/products/{id}` - Удаление товара

### 📂 Categories (Категории)
- `GET /api/categories` - Получение списка категорий
- `POST /api/categories` - Создание новой категории
- `GET /api/categories/{id}` - Получение категории по ID
- `PUT /api/categories/{id}` - Обновление категории
- `DELETE /api/categories/{id}` - Удаление категории

### 💰 Bonuses (Бонусная система)
- `GET /api/bonuses` - Получение бонусов клиента (по телефону или имени)
- `POST /api/bonuses` - Начисление бонусов
- `POST /api/bonuses/deduct` - Списание бонусов
- `POST /api/bonuses/update-name` - Обновление имени клиента
- `GET /api/bonuses/all` - Получение всех бонусных счетов (админ)

### 📊 Sales (Продажи)
- `GET /api/sales` - Получение списка продаж
- `POST /api/sales` - Создание новой продажи

### 👥 Staff (Сотрудники)
- `GET /api/sales-staff` - Получение списка сотрудников продаж
- `POST /api/sales-staff` - Создание нового сотрудника
- `GET /api/sales-staff/{id}` - Получение сотрудника по ID
- `PUT /api/sales-staff/{id}` - Обновление данных сотрудника
- `DELETE /api/sales-staff/{id}` - Удаление сотрудника

### 📨 Contact (Обратная связь)
- `GET /api/contact` - Получение обращений (админ)
- `POST /api/contact` - Отправка нового обращения

### 📁 Upload (Загрузка файлов)
- `POST /api/upload` - Загрузка изображений в Cloudinary
- `DELETE /api/upload` - Удаление изображений из Cloudinary

### 💬 Quotes (Цитаты дня)
- `GET /api/quotes` - Получение случайной активной цитаты
- `GET /api/quotes/all` - Получение всех цитат (админ)
- `POST /api/quotes` - Создание новой цитаты
- `PUT /api/quotes/{id}` - Обновление цитаты
- `DELETE /api/quotes/{id}` - Удаление цитаты

### 📈 Analytics (Аналитика)
- `GET /api/qr-analytics` - Получение аналитики QR кодов
- `POST /api/qr-analytics` - Регистрация сканирования QR кода

## Аутентификация

API использует два типа аутентификации:

### Администратор
- Базовая аутентификация для доступа к админ функциям
- Логин/пароль настраиваются через переменные окружения

### Сотрудники продаж
- Аутентификация через базу данных
- Хешированные пароли с использованием bcrypt

## Модели данных

### Product (Товар)
```json
{
  \"name\": \"string | {ru: string, kk: string, en: string}\",
  \"description\": \"string | {ru: string, kk: string, en: string}\", 
  \"price\": \"number\",
  \"salePrice\": \"number (optional)\",
  \"category\": \"string\",
  \"sku\": \"string\",
  \"ageRange\": \"string\",
  \"inStock\": \"boolean\",
  \"images\": [\"string\"],
  \"videoUrl\": \"string (optional)\"
}
```

### Category (Категория)
```json
{
  \"name\": {
    \"ru\": \"string\",
    \"kk\": \"string\", 
    \"en\": \"string\"
  },
  \"description\": {
    \"ru\": \"string\",
    \"kk\": \"string\",
    \"en\": \"string\"
  }
}
```

### Sale (Продажа)
```json
{
  \"items\": [{
    \"productId\": \"string\",
    \"productName\": \"string\",
    \"quantity\": \"number\",
    \"price\": \"number\",
    \"total\": \"number\"
  }],
  \"totalAmount\": \"number\",
  \"staffId\": \"string\",
  \"customerPhone\": \"string (optional)\",
  \"bonusesUsed\": \"number (optional)\"
}
```

## Особенности API

### Мультиязычность
- Продукты и категории поддерживают русский (ru), казахский (kk) и английский (en) языки
- Возможно использование как простых строк, так и объектов с переводами

### Возрастная фильтрация
- Поддержка как старого формата возрастных групп (\"6-8\", \"9-12\", \"13+\")
- Так и нового формата с указанием конкретного возраста ребенка (например, \"10\")

### Бонусная система
- Автоматическое создание бонусного счета при первом обращении
- Гибкий поиск по номеру телефона (игнорирует форматирование)
- Поддержка поиска по полному имени

### Пагинация
- Стандартная пагинация для списков товаров и продаж
- Параметры: `page`, `limit`
- Возвращает информацию о текущей странице, общем количестве и наличии следующих/предыдущих страниц

### Фильтрация товаров
Доступны следующие фильтры:
- По категории/подкатегории
- По цене (минимальная/максимальная)
- По наличию на складе
- По уровню сложности
- По возрастной группе
- Текстовый поиск по названию, описанию, тегам, SKU

### Загрузка изображений
- Интеграция с Cloudinary для хранения изображений
- Поддержка форматов: JPEG, PNG, GIF, WebP
- Максимальный размер файла: 10MB
- Групповая загрузка и удаление

## Коды ответов

- `200` - Успешный запрос
- `201` - Ресурс успешно создан
- `400` - Ошибка в параметрах запроса
- `401` - Неавторизованный доступ
- `404` - Ресурс не найден
- `409` - Конфликт (дублирование данных)
- `500` - Внутренняя ошибка сервера

## Примеры использования

### Получение товаров с фильтрацией
```bash
GET /api/products?category=Электроника&minPrice=1000&maxPrice=5000&page=1&limit=10
```

### Создание нового товара
```bash
POST /api/products
Content-Type: application/json

{
  \"name\": {
    \"ru\": \"Набор для изучения электроники\",
    \"kk\": \"Электроника үйрену жиынтығы\",
    \"en\": \"Electronics Learning Kit\"
  },
  \"price\": 15000,
  \"category\": \"Электроника\",
  \"sku\": \"ELK-001\",
  \"ageRange\": \"8-12\"
}
```

### Поиск бонусов по телефону
```bash
GET /api/bonuses?phone=+7(777)123-12-12
```

### Создание продажи
```bash
POST /api/sales
Content-Type: application/json

{
  \"items\": [{
    \"productId\": \"507f1f77bcf86cd799439011\",
    \"productName\": \"Набор Arduino\",
    \"quantity\": 1,
    \"price\": 15000,
    \"total\": 15000
  }],
  \"totalAmount\": 15000,
  \"staffId\": \"507f1f77bcf86cd799439012\",
  \"customerPhone\": \"+7 (777) 123-12-12\"
}
```

## Разработка и тестирование

1. Запустите сервер разработки:
```bash
npm run dev
```

2. Откройте Swagger UI:
```
http://localhost:3000/api/docs
```

3. Используйте интерфейс Swagger для тестирования API endpoints

## Поддержка

Для вопросов по API обращайтесь к команде разработки ZerekLab.